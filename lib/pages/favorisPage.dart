import 'package:flutter/material.dart';

class FavorisPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Favoris'), // Titre de la page
      ),
      body: Center(
        child: Text('Contenu de la page de favoris'), // Contenu de la page d'accueil
      ),
    );
  }
}
